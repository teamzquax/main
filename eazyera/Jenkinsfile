pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls -al'
                sh 'ng build'
            }
        }
        
        stage('Unit Tests') {
            steps {
                sh 'ng test --watch=false --code-coverage'
            }
        }
        
        stage('E2E Tests') {
            steps {
                sh 'ng e2e'
            }
        }
    }
    
    post {
        always {
            echo 'Pipeline execution is complete.'
        }
    }
}
